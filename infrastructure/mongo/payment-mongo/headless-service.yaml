apiVersion: v1
kind: Service
metadata:
  name: payment-mongo
  namespace: app-service-ns
spec:
  clusterIP: None
  selector:
    app: payment-mongo
  ports:
    - port: 27017
      name: payment-mongo