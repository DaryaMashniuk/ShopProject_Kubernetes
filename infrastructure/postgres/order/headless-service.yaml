apiVersion: v1
kind: Service
metadata:
  name: order-postgres
  namespace: app-service-ns
spec:
  clusterIP: None
  selector:
    app: order-postgres
  ports:
    - port: 5432
      name: order-postgres